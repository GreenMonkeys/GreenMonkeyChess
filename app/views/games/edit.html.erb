<div class="booyah-box col-xs-10 col-xs-offset-1">
	<h1>Create a New Game</h1>
	<br />
	<%= simple_form_for @game do |f| %>
    	<%= f.input :name %>
    	<br />
    	<%= f.submit 'Initiate Game', :class => 'btn btn-primary' %>
  	<% end %>
</div>